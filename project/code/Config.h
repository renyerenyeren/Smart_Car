#ifndef _Config_h_
#define _Config_h_

#define    Exposure_Time        45
#define    DefaultMaxBox        15
#define    ART_Exposure_Time    700

#define CLAMP(value, min, max) ((value) <= (min) ? (min) : (value) >= (max) ? (max) : (value))


#endif